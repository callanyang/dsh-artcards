# dsh-artcards 安装教程

## 1. 环境要求

- 已安装 DeepSeek Harness，并初始化 `web` profile；
- Node.js 22 或更高版本；
- 安装时找不到 `dsh` 命令的情况下，需要 pnpm 或 Corepack。

检查主要环境：

```bash
dsh --version
node --version
```

## 2. 安装

把项目放在一个不会移动或删除的目录中，然后执行：

```bash
cd /path/to/dsh-artcards
./install.sh
```

插件会以本地链接方式安装，因此使用期间需要保留当前项目目录。

安装完成后重启 DSH：

```bash
dsh web
```

## 3. 验证和使用

进入 DSH Web，执行一项会创建或修改文件的任务。任务完成后，回复下方应出现产物卡片，并显示文件名、目录和可用的打开方式。

如需确认插件是否注册，可以执行：

```bash
grep -n artcards ~/.dsh/profiles/web/package.json
```

## 4. 卸载

```bash
cd /path/to/dsh-artcards
./uninstall.sh
```

卸载后重启 `dsh web`。卸载脚本可以重复执行。

## 5. 常见问题

### 安装脚本找不到 pnpm

启用 Corepack 提供的 pnpm，然后重新安装：

```bash
corepack enable
./install.sh
```

### 没有出现产物卡片

1. 重启 DSH 并刷新浏览器；
2. 确认 `~/.dsh/profiles/web/package.json` 中包含 `dsh-artcards`；
3. 确认任务真正创建或修改了文件，纯读取任务不会产生卡片。

### 只有默认打开方式

请使用 `127.0.0.1` 或 `localhost` 访问 DSH Web，并确保 DSH 与浏览器运行在同一台有桌面环境的机器上。
